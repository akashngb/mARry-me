{
  "name": "camera-lucy-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Proxy for the camera app: Claude command-rewrite + fal realtime token minting. Holds the API keys.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
